name: compute
description: Compute capability — submit batch/stream jobs to a pluggable engine (local/beam/spark/flink/databricks).
priority: 50
backend:
  package: github.com/togo-framework/compute
migrations: []
frontend: { dir: "" }
commands: []
